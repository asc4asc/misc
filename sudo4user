#!/bin/bash

echo "Make this user easy to become root"
echo $USER 
ls /etc/sudoers.d
sudo echo ${USER} "ALL=(ALL) NOPASSWD:ALL" >/etc/sudoers.d/sudo4${USER}
echo "Sorry not tested/implemented yet!"
echo "remove with simple rm /etc/sudoers.d/sudo4${USER}"
